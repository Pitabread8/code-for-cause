import { StyleSheet, Text, View, Button } from 'react-native';

const SolutionsScreen = ({ navigation, route }) => {
    return <Text>Here are the solutions</Text>;
};

export default SolutionsScreen;
